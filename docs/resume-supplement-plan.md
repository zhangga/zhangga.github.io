# HTML 简历补充方案（已确认）

本方案已确认。项目规模、负责范围、多模式部署和 AI 自动归因内容已同步至在线版与打印版 HTML；打印版已整理为两页，保留九段工作经历的公司、职位和时间线。

## 1. 定位与表达重点

建议保留“游戏服务器主程 / 技术专家”的求职方向，以“UE DS 架构、性能与成本治理”为主要技术特色，明确战场服架构责任、组内工作分工和代码质量基线建设。

建议页首标题：

> 游戏服务器主程 / 技术专家

建议方向说明：

> UE DS 架构与性能优化 · 游戏服高可用 · AI 研发自动化

建议个人简介：

> 14+ 年游戏服务器研发经验，覆盖 SLG、MMO、沙盒与 UE DS。现任《雾影猎人》DS 负责人，负责战场服架构设计、性能与成本治理，以及组内工作分工和代码质量基线建设；具备多种运行模式与多环境部署实践，建设基于 Harness / gamecli 的 AI 自动分析与归因能力。

项目规模集中放在成果卡片和最新项目概况中；个人简介承担定位作用。

## 2. 新增规模数据的写法

| 已提供的数据 | 建议用语 | 表述范围 |
| --- | --- | --- |
| 峰值在线 10W+ | 峰值在线 10 万+ | 按最新项目的业务规模呈现，不直接等同于单个 DS 的承载量 |
| 6 大战区 | 覆盖 6 大战区 | 保留业务分区原称，不转换为机房数或地理区域数 |
| 200+ Pod，属于 DS 实例 | 200+ DS Pod | 不等同于物理服务器数量或调度管理服务 Pod 数量 |
| 高峰期 20W+ 战场调度 | 高峰期调度 20 万+战场 | 按高峰期调度规模表述，不改写为日调度量或瞬时并发量 |

项目概况审阅稿：

> 项目：《雾影猎人》｜DS 负责人  
> 业务规模：峰值在线 10 万+，覆盖 6 大战区；管理 200+ DS Pod，高峰期调度 20 万+战场。  
> 负责范围：战场服架构设计与成本优化，承担组内日常工作分工及代码质量基线建设。

以上用语采用用户确认的数据范围。高峰期的具体观察时长可保留在面试材料中，以便解释调度规模。

## 3. 关键成果卡片调整

保持六张卡片，将原“从 0 到 1”卡片升级为带业务规模的架构成果。

| 卡片 | 主数字或结论 | 建议说明 |
| --- | --- | --- |
| 场景服架构与调度 | 支撑 10 万+峰值在线 | 从零搭建 UE DS 战场管理服务，覆盖 6 大战区，高峰期调度 20 万+战场。 |
| 低谷时段服务器成本 | 降低 50%+ | 结合 ARM 构建发布管线与 Agones 弹性伸缩优化资源配置。 |
| 营地 DS 多实例 | 单核承载 ×10 | 单核承载能力达到原来的 10 倍，内存占用降低 50%。 |
| DS 性能治理 | 低帧率占比下降约 97.4% | 10 秒帧率统计中，P95 帧率低于 25 FPS 的占比由 4.22% 降至 0.108%。 |
| 游戏服高可用 | 多活 / 主备 | 推进多节点多活、单节点主备改造，完成 Actor 框架迁移及存储性能优化。 |
| AI 自动诊断 | 故障自动归因 | 基于 Harness / gamecli，自动分析并归因战场崩溃、错误日志与卡顿问题，支持性能问题主动发现。 |

卡片主数字优先保留业务峰值，以高峰期战场调度规模作补充；完整规模及 200+ DS Pod 信息放在项目概况。

## 4. 最新工作经历建议稿

建议在项目概况中单列负责范围，将当前九条工作内容整理为以下七条。先展示架构责任，再展示部署设计、性能、成本和自动诊断成果。

1. **场景服架构与调度：**作为《雾影猎人》DS 负责人，从零搭建战场管理服务，支撑峰值在线 10 万+、覆盖 6 大战区的游戏业务；管理 200+ DS Pod，高峰期调度 20 万+战场，负责 DS 生命周期、发布调度与动态容量规划。
2. **游戏服高可用与框架演进：**推进多节点多活和单节点主备改造，完成 Actor 框架迁移及存储性能优化。
3. **多模式部署与资源利用：**设计并实现独立进程、fork、多实例部署方案，兼顾日常研发、组内测试和线上运行的不同需求，平衡调试便利性与线上资源效率；支持多版本研发部署及 Editor DS 调试。营地 DS 单核承载能力达到原来的 10 倍，内存占用降低 50%；通过战场 fork 模式使战场承载能力提升 30%。
4. **弹性调度与成本治理：**完整搭建 ARM 构建发布管线，基于 Agones 支持自动扩缩容与原地滚动更新，低谷时段服务器成本降低 50% 以上。
5. **性能治理与可观测性：**搭建场景服监控体系并接入 TypeScript Trace，治理低帧率与长帧问题；线上 10 秒帧率统计中，P95 帧率低于 25 FPS 的占比由 4.22% 降至 0.108%，相对下降约 97.4%；统计期内，超过 100ms 的长帧占比为 0%。
6. **复制与裁剪优化：**接入 Iris 网络复制并实现遮挡剔除，其中 Iris 接入后 CPU 消耗降低 20%。
7. **AI 自动诊断与研发效能：**搭建场景服 Harness 与 gamecli 工具，自动分析并归因战场崩溃、错误日志与卡顿问题，支持逻辑 Bug 查询和性能问题主动发现；接入 AI CodeReview，支撑代码质量建设。

在线版可保留 P99 帧率指标作为补充证据。打印版优先保留 P95、长帧指标和部署设计取舍，控制单条内容长度。

三项新增信息的精简版，可直接用于打印版：

> **架构与团队职责：**负责战场服架构设计、成本优化、组内日常工作分工及代码质量基线建设。  
> **多模式部署：**支持独立进程、fork、多实例部署，兼顾日常研发、组内测试和线上运行需求，平衡研发调试便利性与线上资源效率。  
> **AI 自动诊断：**基于 Harness / gamecli，自动分析并归因战场崩溃、错误日志及卡顿问题，支持性能问题主动发现。

## 5. 已补齐的信息与可选佐证

| 维度 | 已有事实 | 文案如何体现 |
| --- | --- | --- |
| 负责范围 | 战场服架构设计、成本优化、日常分工、代码质量基线 | 单列职责范围，明确技术负责人实际承担的工作 |
| 部署设计 | 多种运行模式兼顾日常研发、组内测试、线上运行 | 写明多模式部署与环境需求之间的取舍 |
| AI 能力 | 自动分析并归因战场崩溃、错误日志和卡顿 | 从工具名称展开到具体诊断对象与自动归因能力 |

这些信息已能形成完整文案。以下材料可用于进一步增强面试中的技术论证：

- 选择一种部署模式的具体原因，以及需要解决的兼容、资源或调试问题。
- 多实例 10 倍承载提升的主要技术改动与测试条件。
- 一次 AI 归因的实际输入、输出与人工核验过程；有记录时补充耗时变化。
- 代码质量基线覆盖的具体规则和执行方式。
- 趣加时期的一项代表性框架改造或技术攻坚成果。
- 实际使用 C++、TypeScript、Kubernetes 等技术的范围和深度。

可以按以下格式提供原始材料，无需先整理成简历语言：

> 项目 / 模块：  
> 原来的瓶颈或故障：  
> 我负责的设计和实现：  
> 最关键的一项技术改动：  
> 为什么选择这个方案：  
> 优化前后结果及统计条件：

## 6. 内容与版面落地情况

1. 已使用确认后的规模数据，更新页首定位、项目概况与第一张成果卡片。
2. 已写入负责范围、多模式部署和 AI 自动归因内容，最新工作经历整理为七条。
3. 在线版保留完整履历；打印版精简较早职责描述，保留公司、日期、职位和核心项目。
4. 打印版已整理为两页：第一页在姓名与求职方向下突出教育背景，再展示核心成果及最新经历；第二页展示既往经历、技术栈和证书荣誉。在线版教育背景同步移至页首，突出学校、学历和专业。
5. 核心技术精简为四组：Java / Go、分布式 / Actor、UE DS、MySQL / Redis / MongoDB。
6. 趣加代表案例、具体技术难点和 AI 效率数据，可在提供真实材料后继续补充。

现有 10 倍承载、低谷降本 50%+、低帧率占比下降约 97.4% 和 100ms 长帧指标，按用户最新确认值使用。面试材料中分别保留对应的测量条件和统计时段。
